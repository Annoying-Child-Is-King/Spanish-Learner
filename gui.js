// Previous content of the file...

// Updating grammarBody element
const grammarBody = document.getElementById("grammar-body") || document.createElement("div");

// Previous content of the file...

// Updating the doTranslate error handler
if (error) {
    console.error("Translation error:", error);
    output = document.getElementById("output"); // Changed from outputBox to output
}

// Following content of the file...